# Codex manual bridge template

Template only; never overwrite or install host configuration automatically. The operator supplies `<PACK>/docs/agent-contract.md`, the proposal role and one bounded job context.

Produce a proposal with no tools or target access. No Git/build/tests/network, skill loading, subagents or session reuse. Read-only sandboxing alone is insufficient; native dispatch is unavailable. Do not bypass sandbox/approval rules. The configured serial script integrator owns all target effects; local verification is not Sonar confirmation.
