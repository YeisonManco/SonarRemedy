# OpenCode manual bridge template

Template only; no automatic installation or host autodiscovery. Preserve stricter host settings. The operator supplies `<PACK>/docs/agent-contract.md`, the proposal worker role, and one bounded `job.json` to a fresh tool-free context.

Return a proposal only: no tools, target edits, Git/build/tests/network, skill loading or child agents. Native queue execution is unavailable; a deny-all Markdown template is not proof of effective startup permissions. Use manual files when runtime restrictions are unverified. Only the separately authorized serial script integrator applies changes.
